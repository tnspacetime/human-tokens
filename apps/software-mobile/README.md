# Software Tokens Mobile

The Expo mobile client for Software Tokens.

## Development

Install dependencies and start Expo:

```bash
bun install
bun start
```

Use `bun android`, `bun ios`, or `bun web` for a specific platform.
